[project]
name = "openhive-engine"
version = "0.1.0"
description = "OpenHive policy engine — observation, training, execution"
requires-python = ">=3.11"
dependencies = [
    "hud-python>=0.6.6",
    "openai>=1.0",
    "networkx>=3.0",
    "numpy>=1.26",
    "pydantic>=2.0",
    "websockets>=12.0",
    "browser-use>=0.1",
]

[tool.uv]
package = false
